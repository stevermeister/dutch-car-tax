import { TruckIconDirective } from './truck-icon.directive';

describe('TruckIconDirective', () => {
  it('should create an instance', () => {
    const directive = new TruckIconDirective();
    expect(directive).toBeTruthy();
  });
});
